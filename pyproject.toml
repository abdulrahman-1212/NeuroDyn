[project]
name = "your-package"
version = "0.1.0"
description = "A tool for Scientific Machine Learning"
authors = [{ name = "Your Name", email = "you@email.com" }]
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "torch>=2.4",
    "jax>=0.4",
    "numpy",
    "scipy",
    "pandas",
    "matplotlib",
    # Add more as needed
]

[tool.setuptools.packages.find]
where = ["src"]